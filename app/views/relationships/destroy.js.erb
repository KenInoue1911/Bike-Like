$(".follow-count").html("<%= j(link_to @user.followers.count, user_followers_path(@user.id)) %>");
console.log("ss3")
$(".follow_<%= @user.id %>").html("<%= j(render 'relationships/follow-btn', user: @user) %>");
